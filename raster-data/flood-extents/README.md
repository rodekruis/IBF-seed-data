# Flood extent maps

Nation-wide flood extent rasters per return period. Used in floods pipeline to create event flood extent and calculate exposure.

Source files are in `/tif`. These are converted into png and metadata using `convert_flood_extents_to_png.py` script in /IBF repo.
